{
  "name": "fundraising",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "publish": "lerna run publish",
    "fix": "rm -rf **/websocket/.git",
    "clean": "lerna clean"
  },
  "devDependencies": {
    "lerna": "^3.4.3"
  },
  "dependencies": {
    "@aragon/os": "^4.2.0"
  }
}
